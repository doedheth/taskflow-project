{
  "launch_options": {
    "args": ["--no-sandbox", "--disable-setuid-sandbox"]
  },
  "mermaid_options": {
    "theme": "default"
  },
  "scripts": [
    "https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"
  ],
  "css": ".mermaid { background: transparent; }"
}
